build_settings:
  ignore:
    - "tests"
    - "libs"
    
test:
  grunt:
    task: "default"
    
complete:
  email:
    default_mailto_address: derek@huement.com
  shell:
    command: "cp -R /var/www/phpci/PHPCI/build/%BUILD% /var/www/huitest"
    
failure:
  email:
    committer: true
    default_mailto_address: derek@huement.com