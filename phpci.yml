build_settings:
  ignore:
    - "tests"
    - "libs"

complete:
  email:
    default_mailto_address: derek@huement.com
  shell:
    command: "cp -R %buildpath%hui /var/www/huitest"
    
failure:
  email:
    committer: true
    default_mailto_address: derek@huement.com