build_settings:
  ignore:
    - "tests"
    - "libs"
    
test:
  grunt:
    task: "default"
    
complete:
  email:
    default_mailto_address: derek@huement.com
  shell:
    command: "cp -R %BUILD_PATH%%BUILD% /var/www/huitest"
    
failure:
  email:
    committer: true
    default_mailto_address: derek@huement.com