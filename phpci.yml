build_settings:
  ignore:
    - "tests"
    - "libs"
    
test:
  grunt:
    task: "docsbuild"
 
complete:
  email:
    default_mailto_address: derek@huement.com
  shell:
    command: "cp -R %buildpath% /var/www/huitest"
    
failure:
  email:
    committer: true
    default_mailto_address: derek@huement.com